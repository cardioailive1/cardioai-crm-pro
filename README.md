# Cardio AI CRM Pro

A production-ready, full-stack version of the Cardio AI sales & clinical CRM.
The original was a single static HTML file backed by `localStorage`; this is a
dynamic platform with a Node/Express backend, **Google Workspace SSO**, a
**PostgreSQL** database, and a server-side AI proxy — deployable on
[Render.com](https://render.com) with one Blueprint.

It uses the **same authentication and deployment architecture** as the Cardio AI
Operations platform: Passport + Google OAuth, domain-restricted sign-in,
Postgres-backed sessions, and a generic document store seeded on first boot.

---

## What changed vs. the static file

| Area | Before | Now |
| --- | --- | --- |
| Persistence | Browser `localStorage` (per device) | PostgreSQL, shared across the team |
| Auth | Client-side 4-digit PIN (cosmetic) | Google Workspace SSO, domain-restricted |
| AI Assistant | Anthropic key in the browser | Server-side proxy (`ANTHROPIC_API_KEY`) |
| Roles | PIN → role map | Signed-in Google email → role map |
| Hosting | Open the file | Node web service + managed Postgres on Render |

The entire CRM UI — dashboard, contacts, pipeline (Kanban), tasks,
notifications, activity feed, sequences, Salesforce view, analytics, and all the
other tabs — is preserved. Role-based navigation gating still applies, now keyed
to the authenticated Google identity.

---

## Architecture

```
Browser (index.html SPA)
   │  fetch() with session cookie
   ▼
Express server (server.js)
   ├─ /auth/google, /auth/google/callback, /auth/logout   ← Passport + Google OAuth
   ├─ /login (Google sign-in)   /  (SPA, requires auth)
   ├─ /api/me, /healthz
   ├─ /api/bootstrap            ← all collections in one read
   ├─ /api/state (PUT)          ← debounced full-state save from the SPA
   ├─ /api/dashboard            ← computed metrics
   ├─ /api/:collection CRUD     ← contacts, deals, tasks, notifications, activities, sequences
   └─ /api/ai/chat              ← Anthropic proxy (key stays server-side)
        │
        ▼
   storage.js  →  PostgreSQL  (documents + singletons + session tables)
                  (falls back to a local data.json when DATABASE_URL is unset)
```

Data is stored as JSONB documents in a `documents` table keyed by
`(collection, id)` — real, queryable Postgres rows, not an opaque blob. Swap in
a stricter normalized schema later without changing the API surface.

---

## Deploy to Render (Blueprint)

### 1. Push this folder to a Git repo (GitHub/GitLab/Bitbucket)

```bash
git init && git add . && git commit -m "Cardio AI CRM Pro"
git remote add origin <your-repo-url> && git push -u origin main
```

### 2. Create the Google OAuth credentials

1. Go to **Google Cloud Console → APIs & Services → Credentials**.
2. **Create Credentials → OAuth client ID → Web application**.
3. Under **Authorized redirect URIs**, add:
   `https://<your-service-name>.onrender.com/auth/google/callback`
   (You can add `http://localhost:3000/auth/google/callback` too for local dev.)
4. Copy the **Client ID** and **Client Secret**.

> Tip: if your company uses Google Workspace, restricting `ALLOWED_EMAIL_DOMAIN`
> to `cardioailive.com` ensures only employees can sign in.

### 3. Create the Blueprint on Render

1. In Render: **New + → Blueprint**, select your repo.
2. Render reads `render.yaml` and provisions the **web service** + **Postgres
   database** automatically.
3. When prompted, paste the `sync: false` secrets:
   - `GOOGLE_CLIENT_ID`
   - `GOOGLE_CLIENT_SECRET`
   - `ANTHROPIC_API_KEY` (optional — enables the AI Assistant)
4. Click **Deploy Blueprint**.

On first boot the server creates its tables and seeds them from `seed.json`.
`DATABASE_URL`, `SESSION_SECRET`, and the public URL are wired up for you.

### 4. Sign in

Visit your service URL → you're redirected to `/login` → **Sign in with Google**.
Only accounts on the allowed domain can enter.

---

## Environment variables

| Variable | Required | Description |
| --- | --- | --- |
| `DATABASE_URL` | yes (prod) | Postgres connection string (set by the Blueprint) |
| `SESSION_SECRET` | yes | Session cookie signing secret (auto-generated by Render) |
| `GOOGLE_CLIENT_ID` | yes | Google OAuth client ID |
| `GOOGLE_CLIENT_SECRET` | yes | Google OAuth client secret |
| `ALLOWED_EMAIL_DOMAIN` | yes | Comma-separated allowed domains (default `cardioailive.com`) |
| `ALLOWED_EMAILS` | no | Comma-separated extra allowed emails (overrides domain check) |
| `ANTHROPIC_API_KEY` | no | Enables the AI Assistant proxy |
| `ANTHROPIC_MODEL` | no | Model id (default `claude-sonnet-4-6`) |
| `BASE_URL` | no | Overrides the auto-detected public URL |
| `GOOGLE_CALLBACK_URL` | no | Overrides `<BASE_URL>/auth/google/callback` |
| `PORT` | no | Defaults to 3000 (Render sets this automatically) |

---

## Local development

```bash
cp .env.example .env       # fill in Google creds (and optionally ANTHROPIC_API_KEY)
npm install
npm run dev                # node --watch server.js
# → http://localhost:3000
```

Without `DATABASE_URL` the app uses a local `data.json` file store, so you can
run with zero database setup. With Google creds configured, set the redirect URI
`http://localhost:3000/auth/google/callback` in Google Cloud Console.

To run fully locally **with** Postgres, set `DATABASE_URL` in `.env`, e.g.
`postgres://postgres:postgres@localhost:5432/cardio_ai_crm`.

---

## Mapping employees to roles

`public/index.html` contains the `TEAM_MEMBERS` roster and a `ROLES` permission
matrix (carried over from the original). When someone signs in, their Google
email is matched (case-insensitive) to a roster entry and they inherit that
member's role, which drives the sidebar navigation gating.

- **Known emails** (e.g. `sampson@cardioailive.com`) get their assigned role.
- **Other allowed-domain emails** get `DEFAULT_ROLE` (currently `'ceo'` /
  full access). Change `DEFAULT_ROLE` near the top of the script, or add the
  person to `TEAM_MEMBERS`, to restrict them.

> Role gating is a UX convenience. The real security boundary is Google
> domain-restricted authentication enforced on the server.

---

## Inspecting / resetting data

Connect with the database's **External Database URL** using `psql`:

```bash
psql "EXTERNAL_DATABASE_URL"
\dt                                            -- documents, singletons, session
SELECT collection, count(*) FROM documents GROUP BY collection;
```

To re-seed from scratch: `TRUNCATE documents, singletons;` then restart the
service — it reseeds from `seed.json` when `documents` is empty.

---

## Project structure

```
cardio-ai-crm/
├── render.yaml          # Render Blueprint (web service + Postgres)
├── package.json
├── .env.example
├── .gitignore
├── server.js            # Express app: Google OAuth, REST API, AI proxy
├── storage.js           # Postgres (or file) document store + seeding
├── seed.json            # initial CRM data (contacts, deals, tasks, …)
└── public/
    └── index.html       # the CRM single-page app (API-backed)
    └── login.html       # Google sign-in page (served at /login)
```
